# Data models for openreadersbibles.org

## Building after changes to `assets`
Run this command to convert the files in `assets` into JSON format, so they can be loaded as variables at build time.

```
npm run build
```